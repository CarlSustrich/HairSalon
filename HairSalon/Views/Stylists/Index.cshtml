@{
  Layout = "_Layout";
}
@using HairSalon.Models;

<h1>All Stylists</h1>
@if(Model.Count == 0)
{
  <h2>No Stylists Added Yet</h2>
}

<ul>
@foreach (Stylist item in Model)
{
  <li>@Html.ActionLink($"{item.StylistName}", "Details", "Stylists", new { stylistId=item.StylistId})</li>
}
</ul>
